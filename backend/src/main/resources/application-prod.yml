spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

app:
  # 운영 환경에서는 JWT_SECRET을 반드시 Railway 환경변수로 넣어야 한다.
  # 로컬 기본값(application.yml의 placeholder)으로 조용히 기동되지 않도록 기본값을 두지 않는다.
  jwt:
    secret: ${JWT_SECRET}
